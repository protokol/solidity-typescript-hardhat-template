name: TESTS

on:
  push:
    branches:
      - "main"
      - "develop"
  pull_request:
    types: [ready_for_review, synchronize, opened]

jobs:
  tests:
    name: TESTS
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install
        run: npm install
    
      - name: Setup .env
        uses: SpicyPizza/create-envfile@v1
        with:
            envkey_RINKEBY_RPC_URL: ${{ secrets.SECRET_RINKEBY_URL }}
            envkey_PRIVATE_KEY: ${{ secrets.SECRET_PRIVATE_KEY }}
            envkey_PINATA_API_KEY: 'API_KEY'
            envkey_PINATA_API_SECRET: 'API_SECRET'
            env_key_NFT_CONTRACT_ADDRESS: '0x123lsafddws0fsd2134dsa0wdfsd0fsdf0fsdfs'
            file_name: .env

      - name: Compile
        run: npm run compile
